# Required for AI Insights feature (optional — app works without it using fallback responses)
GEMINI_API_KEY=your_gemini_api_key_here

# Required for live gold/silver prices (optional — app works without it using fallback rates)
METAL_PRICE_API_KEY=your_metal_price_api_key_here
